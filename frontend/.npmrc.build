# Build dependency settings
legacy-peer-deps=true
package-lock=true
save-exact=false

# Registry configuration
registry=https://registry.npmjs.org/

# Security settings
audit-level=moderate
fund=false

# Build optimization - include all deps for building
production=false
omit=

