# Ensure consistent dependency resolution
legacy-peer-deps=true
package-lock=true
save-exact=false

# Registry configuration
registry=https://registry.npmjs.org/

# Security settings
audit-level=moderate
fund=false

# Build optimization
# production=false  # Removed to fix npm warning

