<source>
  @type tail
  path /var/log/fluentd/*.log
  pos_file /var/log/fluentd/fluentd.log.pos
  tag disaster-response.*
  format json
</source>

<match disaster-response.**>
  @type stdout
</match>

